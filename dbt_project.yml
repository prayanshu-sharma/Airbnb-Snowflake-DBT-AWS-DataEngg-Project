
name: 'dbt_snowflake_aws_project'
version: '1.0.0'


profile: 'dbt_snowflake_aws_project'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  dbt_snowflake_aws_project:
    # Config indicated by + and applies to all files under models/example/
    example:
      +materialized: view

models:
  dbt_snowflake_aws_project:
    bronze:
      +materialized: table
      +schema: bronze
  dbt_snowflake_aws_project:
    silver:
      +materialized: table
      +schema: silver
  dbt_snowflake_aws_project:
    gold:
      +materialized: table
      +schema: gold
      ephemeral:
        +materialized: ephemeral
      

